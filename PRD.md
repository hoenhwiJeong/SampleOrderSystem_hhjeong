# PRD.md — S-Semi 반도체 시료 생산주문관리 시스템

## 1. 개요

### 1.1 목적

가상의 반도체 회사 **S-Semi**는 주문량 급증으로 인해 엑셀·메모장 기반의 기존 관리 방식에서 오류가 잦아졌다.
본 시스템은 시료 등록부터 주문 접수·승인·생산·출고까지의 전 과정을 **콘솔 기반**으로 체계화한다.

### 1.2 역할 구분

| 역할 | 담당 업무 |
|------|-----------|
| 주문 담당자 | 고객 요청을 받아 주문서 작성, 주문 접수 |
| 생산 담당자 | 주문 승인·거절, 시료 등록, 생산라인 관리 |

### 1.3 시스템 특성

- **콘솔 기반** 대화형 인터페이스
- **JSON 파일 영속성** — 앱 재시작 후에도 데이터 유지
- **단일 생산라인** — FIFO 방식으로 주문을 순차 처리
- 실시간 타이머 없음 — 생산 완료는 메뉴에서 수동 트리거

---

## 2. 데이터 모델

### 2.1 Sample (시료)

```
id               string    시료 고유 ID (예: S-001)
name             string    시료명 (예: 실리콘 웨이퍼-8인치)
avgProductionTime double   평균 생산시간 (단위: min/ea)
yieldRate        double    수율 0.0~1.0 (정상품 / 총생산)
stock            int       현재 재고 수량 (ea)
```

### 2.2 Order (주문)

```
id               string        주문 ID — ORD-YYYYMMDD-NNNN
sampleId         string        주문 시료 ID
customerName     string        고객명
quantity         int           주문 수량 (ea)
status           OrderStatus   주문 상태
```

### 2.3 OrderStatus

| 값 | 의미 | 모니터링 포함 |
|----|------|:---:|
| RESERVED | 주문 접수 대기 | O |
| PRODUCING | 생산 중 (재고 부족) | O |
| CONFIRMED | 출고 대기 (재고 확보) | O |
| RELEASED | 출고 완료 | O |
| REJECTED | 주문 거절 | X |

### 2.4 주문 ID 생성 규칙

```
형식: ORD-YYYYMMDD-NNNN
예시: ORD-20260612-0001

- YYYYMMDD : 주문 접수 날짜
- NNNN     : 당일 4자리 순번 (0001부터 시작)
```

---

## 3. 주문 상태 흐름

```
                   ┌──────────────────────────────────────────┐
                   │                                          │
[주문 접수]         │    [승인 - 재고 충분]                      │
  → RESERVED ──────┼──────────────────────→ CONFIRMED ────────┼──→ RELEASED
                   │                                          │    [출고 처리]
                   │    [승인 - 재고 부족]   [생산 완료]         │
                   └──────────────────────→ PRODUCING ────────┘

  RESERVED ──[거절]──→ REJECTED  (모니터링 제외)
```

**상태 전이 규칙:**
- `RESERVED` → `CONFIRMED` : 승인 시 재고 ≥ 주문 수량
- `RESERVED` → `PRODUCING` : 승인 시 재고 < 주문 수량 (생산라인 자동 등록)
- `RESERVED` → `REJECTED`  : 거절 처리
- `PRODUCING` → `CONFIRMED`: 생산 완료 처리 시
- `CONFIRMED` → `RELEASED` : 출고 처리 시

---

## 4. 핵심 비즈니스 공식

### 4.1 주문 승인 시 재고 판단

```
부족분 = 주문 수량 - 현재 재고

부족분 ≤ 0  →  재고 충분  →  CONFIRMED
부족분 > 0  →  재고 부족  →  PRODUCING + 생산라인 등록
```

### 4.2 실 생산량 계산

```
실 생산량 = ceil( 부족분 / (수율 × 0.9) )
총 생산시간 = 평균 생산시간(min/ea) × 실 생산량
```

- `× 0.9` : 공정 오차(불량률 10%) 감안 안전 마진
- 실 생산량은 항상 올림(ceil)

**계산 예시:**
```
시료: SiC 파워기판-6인치 (수율 0.92, 평균생산시간 0.8 min/ea)
주문: 200 ea, 현재 재고: 30 ea

부족분      = 200 - 30 = 170 ea
실 생산량   = ceil(170 / (0.92 × 0.9)) = ceil(170 / 0.828) = ceil(205.3) = 206 ea
총 생산시간 = 0.8 × 206 = 164.8 min ≈ 165 min
```

### 4.3 생산 완료 후 재고 반영

```
새 재고 = 기존 재고 + 실 생산량 - 주문 수량
```

**예시 (위 이어서):**
```
새 재고 = 30 + 206 - 200 = 36 ea
```

### 4.4 재고 상태 판단 (모니터링용)

```
CONFIRMED 상태 주문들의 해당 시료 수량 합 = confirmedTotal

stock == 0                  →  고갈
stock < confirmedTotal      →  부족
그 외                        →  여유
```

---

## 5. 메인 메뉴

### 5.1 화면 구성

```
============================================================
     S-Semi 반도체 시료 생산주문관리 시스템
============================================================
 시스템 현황  2026-06-12 09:32:15

 등록 시료   5종    총 재고   1,640 ea
 전체 주문   8건    생산라인   2건 대기
------------------------------------------------------------
 [1] 시료 관리          [2] 시료 주문
 [3] 주문 승인/거절     [4] 모니터링
 [5] 생산라인 조회      [6] 출고 처리
 [0] 종료
------------------------------------------------------------
선택 > _
```

### 5.2 시스템 요약 표시 항목

| 항목 | 설명 |
|------|------|
| 등록 시료 수 | 전체 시료 종류 수 |
| 총 재고 | 전체 시료 재고 합산 |
| 전체 주문 수 | REJECTED 제외 전체 주문 수 |
| 생산라인 대기 | PRODUCING 상태 주문 수 |

---

## 6. 시료 관리 [메뉴 1]

### 6.1 서브 메뉴

```
[1] 시료 등록    [2] 시료 목록    [3] 시료 검색    [0] 뒤로
```

### 6.2 시료 등록

**입력 항목:**

| 항목 | 타입 | 제약 |
|------|------|------|
| 시료 ID | string | 중복 불가, 예: S-006 |
| 시료명 | string | 필수 |
| 평균 생산시간 | double | > 0, 단위: min/ea |
| 수율 | double | 0.0 초과 ~ 1.0 이하 |
| 초기 재고 | int | ≥ 0 |

**처리 흐름:**
1. 입력 값 표시 후 확인 [Y/N]
2. ID 중복 시 오류 메시지 출력 후 재입력
3. 등록 완료 시 "시료 등록 완료." 메시지 출력

### 6.3 시료 목록

- 등록된 모든 시료를 테이블 형식으로 출력
- 5건씩 페이지 단위 표시, [N] 다음 페이지

```
등록 시료 목록 (총 5종)
ID       시료명                  평균 생산시간   수율    현재 재고
S-001    실리콘 웨이퍼-8인치      0.5 min/ea     0.92    480 ea
S-002    GaN 에피택셜-4인치       0.3 min/ea     0.78    220 ea
S-003    SiC 파워기판-6인치       0.8 min/ea     0.92     30 ea
S-004    포토레지스트-PR7         0.2 min/ea     0.95    910 ea
S-005    산화막 웨이퍼-SiO2       0.6 min/ea     0.88      0 ea
```

### 6.4 시료 검색

- 시료명 부분 문자열로 검색 (대소문자 무관)
- 검색 결과 없을 시 "검색 결과가 없습니다." 출력

---

## 7. 시료 주문 [메뉴 2]

### 7.1 주문 접수 입력 항목

| 항목 | 타입 | 제약 |
|------|------|------|
| 시료 ID | string | 등록된 ID만 허용 |
| 고객명 | string | 필수 |
| 주문 수량 | int | > 0 |

### 7.2 처리 흐름

```
1. 시료 ID 입력 → 존재 여부 확인
2. 고객명, 주문 수량 입력
3. 입력 내용 확인 화면 표시
4. [Y] 예약 접수 → 주문 ID 발급, 상태: RESERVED
   [N] 취소
5. 완료 메시지 출력
```

### 7.3 완료 화면 예시

```
예약 접수 완료.

주문번호    ORD-20260612-0001
시료        SiC 파워기판-6인치 (S-003)
고객        삼성전자 파운드리
수량        200 ea
현재 상태   RESERVED

※ 재고 확인은 [3] 승인 메뉴에서 직접 진행하세요.
```

---

## 8. 주문 승인/거절 [메뉴 3]

### 8.1 화면 구성

RESERVED 상태 주문 목록을 표시 후 처리할 주문 선택.

```
승인 대기 중인 예약 목록 (RESERVED)
번호    주문번호        고객          시료                  수량       상태
[1]     ORD-...-0041   LG이노텍      산화막 웨이퍼-SiO2    300 ea    RESERVED
[2]     ORD-...-0042   SK하이닉스    실리콘 웨이퍼-8인치   150 ea    RESERVED
[3]     ORD-...-0043   삼성전자      SiC 파워기판-6인치    200 ea    RESERVED

승인할 번호 > _
```

### 8.2 승인 처리 — 재고 충분

```
재고 확인 중...

시료       실리콘 웨이퍼-8인치     현재 재고  480 ea
주문 수량  150 ea                  재고 충분

[Y] 승인    [N] 주문 거절
선택 > Y

승인 완료.
상태 변경    RESERVED → CONFIRMED
주문번호     ORD-20260612-0042
```

**처리 결과:**
- `sample.stock -= order.quantity`
- `order.status = CONFIRMED`

### 8.3 승인 처리 — 재고 부족

```
재고 확인 중...

시료       SiC 파워기판-6인치     현재 재고   30 ea
주문 수량  200 ea                 부족분     170 ea ← 이 수량만큼 생산

재고 부족. 부족분 170 ea 승인하겠습니까? (실생산량 206 ea / 165 min)

[Y] 승인    [N] 주문 거절
선택 > Y

승인 완료.
상태 변경    RESERVED → PRODUCING
주문번호     ORD-20260612-0043
```

**처리 결과:**
- `order.status = PRODUCING`
- `ProductionTask` 생성 후 생산라인 큐에 enqueue

### 8.4 거절 처리

```
[Y] 승인    [N] 주문 거절
선택 > N

주문 거절 처리 완료.
상태 변경    RESERVED → REJECTED
주문번호     ORD-20260612-0041
```

---

## 9. 모니터링 [메뉴 4]

### 9.1 서브 메뉴

```
[1] 주문량 확인    [2] 재고량 확인    [0] 뒤로
```

### 9.2 주문량 확인

상태별 주문 건수 표시 (REJECTED 제외).

```
상태별 주문 현황
  RESERVED     3건
  CONFIRMED    8건
  PRODUCING    3건  ← 생산라인 대기
  RELEASED    18건
```

### 9.3 재고량 확인

각 시료별 재고 현황 및 상태 표시.

```
재고 현황
시료명                  재고        상태    잔여율
실리콘 웨이퍼-8인치      480 ea      여유     80%
GaN 에피택셜-4인치       220 ea      여유     44%
SiC 파워기판-6인치        30 ea      부족      6%
포토레지스트-PR7         910 ea      여유     91%
산화막 웨이퍼-SiO2         0 ea      고갈      0%
```

**재고 상태 기준:**

| 상태 | 조건 |
|------|------|
| 고갈 | `stock == 0` |
| 부족 | `stock < 해당 시료 CONFIRMED 주문 수량 합` |
| 여유 | 그 외 |

**잔여율**: `stock / (stock + 해당 시료 전체 주문 수량) × 100` (표시 보조용)

---

## 10. 생산라인 조회 [메뉴 5]

### 10.1 화면 구성

```
[5] 생산라인 조회   FIFO 방식
------------------------------------------------------------
생산라인 1개 (단일 라인)    현재 상태: RUNNING
------------------------------------------------------------
[현재 처리 중]

  주문번호  ORD-20260612-0038    시료  SiC 파워기판-6인치
  주문량    80 ea     재고 30 ea  →  부족 50 ea  →  실생산량 61 ea (수율 0.92 / 49 min)
  진행      [=========>    ] 72%    완료 예정 09:49

------------------------------------------------------------
[대기 중인 주문]  (FIFO 순)

  순서   주문번호         시료                   주문량    부족분   실생산량   예상완료
   1     ORD-...-0040    산화막 웨이퍼-SiO2      150 ea   150 ea   190 ea    11:43
   2     ORD-...-0043    SiC 파워기판-6인치      200 ea   170 ea   206 ea    14:28
   3     ORD-...-0044    GaN 에피택셜-4인치      300 ea    80 ea   114 ea    15:02

* 부족분 = 주문량 - 재고,  실생산량 = ceil(부족분 / (수율 × 0.9))
* 선입선출(FIFO) 방식으로 처리됩니다.
------------------------------------------------------------
[C] 현재 작업 생산 완료 처리    [0] 뒤로
선택 > _
```

### 10.2 표시 항목 상세

**현재 처리 중 작업:**

| 항목 | 설명 |
|------|------|
| 주문번호 | 처리 중인 주문 ID |
| 시료명 | 생산 중인 시료명 |
| 주문량 | 고객 주문 수량 |
| 재고 | 승인 당시 재고 수량 |
| 부족분 | 주문량 - 당시 재고 |
| 실 생산량 | `ceil(부족분 / (수율 × 0.9))` |
| 총 생산시간 | `평균생산시간 × 실생산량` |
| 진행률 | 진행 바(Progress Bar) 표시 (콘솔 시뮬레이션) |

**대기 큐 목록:**

| 항목 | 설명 |
|------|------|
| 순서 | FIFO 큐 내 순번 |
| 주문번호 | 주문 ID |
| 시료명 | 시료명 |
| 주문량 | ea |
| 부족분 | ea |
| 실 생산량 | `ceil(부족분 / (수율 × 0.9))` |
| 예상 완료 | 앞선 작업들 총 생산시간 합산 후 계산 |

### 10.3 생산 완료 처리 ([C] 선택)

```
현재 작업을 생산 완료 처리합니다.

주문번호    ORD-20260612-0038
시료        SiC 파워기판-6인치
실 생산량   61 ea

재고 반영: 30 ea + 61 ea - 80 ea = 11 ea
상태 변경: PRODUCING → CONFIRMED

생산 완료 처리되었습니다.
다음 대기 작업을 시작합니다: ORD-20260612-0040
```

**처리 결과:**
- `sample.stock = stock + actualProduction - order.quantity`
- `order.status = CONFIRMED`
- 큐에 다음 작업이 있으면 자동으로 현재 작업으로 전환

---

## 11. 출고 처리 [메뉴 6]

### 11.1 화면 구성

CONFIRMED 상태 주문 목록 표시 후 출고할 주문 선택.

```
출고 가능 주문 (CONFIRMED)
번호    주문번호              고객         시료                  수량
[1]     ORD-20260612-0042    SK하이닉스   실리콘 웨이퍼-8인치   150 ea
[2]     ORD-20260612-0035    DB하이텍     포토레지스트-PR7      400 ea

출고할 번호 > 1
```

### 11.2 출고 완료 화면

```
출고 처리 완료.

주문번호    ORD-20260612-0042
출고 수량   150 ea
처리 일시   2026-06-12 09:34:02
상태        CONFIRMED → RELEASED
```

**처리 결과:**
- `order.status = RELEASED`
- 재고는 승인 시점에 이미 차감되어 있으므로 추가 변경 없음

---

## 12. 예외 처리 및 입력 검증

| 상황 | 처리 방식 |
|------|-----------|
| 존재하지 않는 시료 ID 입력 | "등록되지 않은 시료 ID입니다." 출력 후 재입력 |
| 중복 시료 ID 등록 시도 | "이미 존재하는 시료 ID입니다." 출력 후 재입력 |
| 주문 수량 0 이하 | "주문 수량은 1 이상이어야 합니다." 출력 후 재입력 |
| 수율 범위 초과 | "수율은 0.0 초과 1.0 이하여야 합니다." 출력 후 재입력 |
| RESERVED 주문 없음 (승인 메뉴) | "승인 대기 중인 주문이 없습니다." 출력 |
| CONFIRMED 주문 없음 (출고 메뉴) | "출고 가능한 주문이 없습니다." 출력 |
| 생산라인 대기 없음 | "현재 생산 중인 작업이 없습니다." 출력 |
| 잘못된 메뉴 번호 | "올바른 번호를 입력하세요." 출력 후 재입력 |
| JSON 파일 없음 (최초 실행) | 빈 데이터로 초기화 후 정상 진행 |

---

## 13. 데이터 파일 형식

### 13.1 samples.json

```json
[
  {
    "id": "S-001",
    "name": "실리콘 웨이퍼-8인치",
    "avgProductionTime": 0.5,
    "yieldRate": 0.92,
    "stock": 480
  }
]
```

### 13.2 orders.json

```json
[
  {
    "id": "ORD-20260612-0001",
    "sampleId": "S-003",
    "customerName": "삼성전자 파운드리",
    "quantity": 200,
    "status": "RESERVED"
  }
]
```

---

## 14. 기술 스택 및 아키텍처

### 14.1 레이어 구조

```
View (입출력)
  ↕
Controller (비즈니스 로직)
  ↕
Repository (JSON CRUD)
  ↕
Model (데이터 구조)

Service (ProductionLineService) ← Controller가 직접 사용
```

### 14.2 파일 구성

| 파일 | 역할 |
|------|------|
| `model/Sample.h/.cpp` | 시료 구조체, fromJson/toJson |
| `model/Order.h/.cpp` | 주문 구조체, OrderStatus enum, fromJson/toJson |
| `repository/SampleRepository.h/.cpp` | Sample CRUD + JSON load/save |
| `repository/OrderRepository.h/.cpp` | Order CRUD + 상태별 조회 |
| `controller/SampleController.h/.cpp` | 시료 등록·조회·검색 |
| `controller/OrderController.h/.cpp` | 주문 접수·승인·거절·출고 |
| `service/ProductionLineService.h/.cpp` | 생산 큐 FIFO 관리 |
| `view/MainView.h/.cpp` | 메인 메뉴 + 시스템 요약 |
| `view/SampleView.h/.cpp` | 시료 입출력 화면 |
| `view/OrderView.h/.cpp` | 주문 입출력 화면 |
| `view/MonitorView.h/.cpp` | 모니터링 대시보드 |
| `view/ProductionLineView.h/.cpp` | 생산라인 현황 화면 |
| `util/JsonHelper.h/.cpp` | Load/Save 정적 유틸 |
| `nlohmann/json.hpp` | nlohmann/json v3.11.3 |

### 14.3 빌드 환경

- C++20, Visual Studio 2022 (v145), x64
- `SetConsoleOutputCP(CP_UTF8)` + `SetConsoleCP(CP_UTF8)` — 한글 출력

---

## 15. 유의사항

- **사용 모델**: Sonnet / Effort: Medium — Opus 사용 금지
- **커밋 이력**: 기능 단위로 커밋 메시지를 남기며 개발
- **Repository 공개**: Public으로 제출
- **과제 종료**: `/logout` 으로 Claude Code 로그아웃
